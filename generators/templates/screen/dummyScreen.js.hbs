import React from 'react'
import { ScrollView } from 'react-native'
import * as SC from './{{camelCase name}}.styles'

export const {{pascalCase name}} = (props) => (
  <SC.Container>
    <ScrollView>
      <SC.Text>{{pascalCase name}}</SC.Text>
    </ScrollView>
  </SC.Container>
)
